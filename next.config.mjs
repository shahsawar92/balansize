/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['8zrc3pcc-4000.inc1.devtunnels.msuploads', 'balansize.com', 'staging.balansize.com'],
    },
};

export default nextConfig;
